id: iron_condor
enabled: true
enabled_live: false
risk_weight: 0.25
risk_per_trade_pct: 0.015
max_lots_cap: 3
underlyings: [BTC, ETH]

entry:
  weekday: friday
  open_time_ist: "09:30"
  monthly_cooldown_on_max_loss: true

wings:
  short_delta_min: 0.15
  short_delta_max: 0.25
  short_delta_target: 0.20
  long_delta_min: 0.05
  long_delta_max: 0.10
  long_delta_target: 0.075

credit:
  min_credit_pct_of_width: 0.25

desk:
  min_open_interest: 0
  greeks_required: true

exits:
  profit_take_pct_of_credit: 0.50
  stop_loss_x_credit: 2.0
  force_close_days_before_expiry: 2
  tested_side_cut: true
