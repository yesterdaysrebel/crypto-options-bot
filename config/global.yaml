nav_inr: 100000
usd_inr_rate: 85.0

risk_caps:
  daily_loss_pct: 0.03
  weekly_loss_pct: 0.06
  lifetime_dd_pct: 0.15

trading_window:
  start_ist: "09:00"
  end_ist: "22:00"
  expiry_force_close_ist: "16:45"

concurrency:
  max_total: 3
  max_per_strategy: 1

execution:
  spread_filter_max_pct: 0.08
  maker_limit_timeout_seconds: 30
  slip_bps_directional: 50
  slip_bps_strangle: 50
  slip_bps_condor: 100
  trail_update_throttle_seconds: 5

# Desk gates (IV/OI/greeks/portfolio). Keep enabled: false until dry-run validation.
desk:
  enabled: false
  strict: false
  min_open_interest: 50
  greeks_required: true
  max_abs_net_delta_inr: 15000
  max_abs_net_vega_inr: 8000
  # max_vega_per_trade_inr: 3000
  # max_gamma_per_trade_inr: 1500
  iv_history_min_snapshots: 20
  go_live_min_entry_iv_pct: 0.80
  go_live_entry_iv_lookback_trades: 20
